import React from 'react';

import Menu from '.';

const desc = {
  component: Menu,
  title: 'molecules/Menu',
};

export const Default = (): JSX.Element => {
  return <Menu />;
};

export default desc;
