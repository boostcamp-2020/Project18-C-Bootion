import React from 'react';

import Editor from '.';

const desc = {
  component: Editor,
  title: 'molecules/Editor',
};

export const Default = (): JSX.Element => {
  return <Editor />;
};

export default desc;
