import React from 'react';

import HeaderLink from './HeaderLink';

const desc = {
  component: HeaderLink,
  title: 'Atoms/HeaderLink',
};

export const Default = (): JSX.Element => {
  return <HeaderLink>Link</HeaderLink>;
};

export default desc;
