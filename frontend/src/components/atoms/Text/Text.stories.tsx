import React from 'react';

import Text from '.';

const desc = {
  component: Text,
  title: 'Atoms/Text',
};

export const Default = (): JSX.Element => {
  return <Text />;
};

export default desc;
