import React from 'react';

import HeaderButton from '.';

const desc = {
  component: HeaderButton,
  title: 'Atoms/HeaderButton',
};

export const Default = (): JSX.Element => {
  return <HeaderButton>Button</HeaderButton>;
};

export default desc;
